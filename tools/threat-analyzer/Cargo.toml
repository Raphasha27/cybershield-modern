[package]
name = "threat-analyzer"
version = "0.1.0"
edition = "2021"
description = "CLI tool for analyzing threat events and computing anomaly scores"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
